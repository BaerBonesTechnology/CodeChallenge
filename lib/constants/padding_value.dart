

import 'package:flutter/material.dart';

class PaddingValue{
  static const bottomDefaultPadding = EdgeInsets.fromLTRB(8, 8, 8, 16);
  static const bottomLargePadding = EdgeInsets.fromLTRB(16, 8, 16, 32);
  static const defaultPadding = EdgeInsets.all(16.0);
  static const defaultPaddingHorizontal = EdgeInsets.symmetric(horizontal: 16.0);
  static const small = EdgeInsets.all(8.0);
  static const xSmallVertical = EdgeInsets.symmetric(vertical: 3);
  static const xxSmall =  EdgeInsets.symmetric(vertical: 2.0);
  static const snackBarDefault = EdgeInsets.all(5.0);
}