import 'package:flutter/cupertino.dart';

class PaddingValue{
  static const snackBarDefault = 5.0;
  static const small = 2.0;
  static const bottomLargePadding = EdgeInsets.fromLTRB(16, 8, 16, 32);
  static const defaultPadding = EdgeInsets.all(16.0);
  static const defaultPaddingHorizontal = EdgeInsets.symmetric(horizontal: 16);
}